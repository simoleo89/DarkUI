// Adapted for Nitro_Render_V3 2.1.0 / PixiJS 8.
// The renderer ships an up-to-date WiredFilter — re-export it under the old name
// so existing consumers compile unchanged.
export { WiredFilter as WiredSelectionFilter } from '@nitrots/nitro-renderer';
